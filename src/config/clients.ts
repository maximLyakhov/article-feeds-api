import { ClientCode } from '@root/types/client-code.type';
import { Client } from '@root/types/client.type';

export const ClientsMap: Map<ClientCode, Client> = new Map().set(
  'mailonline',
  '264c0f34a1ded9baa28d8a68e7fcfc93',
);
